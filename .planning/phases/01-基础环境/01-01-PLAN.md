---
phase: 01-基础环境
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - tests/__init__.py
  - tests/test_env_imports.py
autonomous: true
requirements:
  - OSS-02
  - OSS-03

must_haves:
  truths:
    - "运行 pip install -r requirements.txt 无报错"
    - "所有核心包（paddleocr、anthropic、lancedb、watchdog、sentence-transformers）均可 import"
    - "requirements.txt 不含已决策弃用的依赖（agentscope）"
    - "测试文件对每个核心包的 import 均通过"
    - "config/.env.example 存在且包含全部必需配置键"
  artifacts:
    - path: "requirements.txt"
      provides: "完整且干净的依赖清单"
      contains: "anthropic"
    - path: "tests/test_env_imports.py"
      provides: "可自动验证 import 的测试套件"
      exports: ["test_import_anthropic", "test_import_lancedb", "test_import_watchdog", "test_import_paddleocr", "test_import_sentence_transformers", "test_env_example_exists_and_has_required_keys"]
  key_links:
    - from: "requirements.txt"
      to: "tests/test_env_imports.py"
      via: "pip install 后 pytest 验证"
      pattern: "import (anthropic|lancedb|watchdog|paddleocr|sentence_transformers)"
    - from: "config/.env.example"
      to: "tests/test_env_imports.py"
      via: "test_env_example_exists_and_has_required_keys 断言文件存在和键完整性"
      pattern: "ANTHROPIC_API_KEY|LOG_LEVEL|LOG_DIR|LANCEDB_PATH|FEISHU_WEBHOOK_URL"
---

<objective>
修正 requirements.txt 并通过 TDD 验证所有核心包在 Windows 11 环境可正常导入。

Purpose: 确保任何人克隆仓库后 pip install 一步成功，且核心包可用——这是后续所有阶段的基础前提。
Output: 干净的 requirements.txt（移除弃用的 agentscope）+ 自动化 import 验证测试套件 + .env.example 完整性验证。
</objective>

<execution_context>
@C:/Users/EDY/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/EDY/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/个人项目/mutiagent_trea/.planning/PROJECT.md
@D:/个人项目/mutiagent_trea/.planning/ROADMAP.md
@D:/个人项目/mutiagent_trea/.planning/STATE.md
@D:/个人项目/mutiagent_trea/requirements.txt
</context>

<tasks>

<task type="tdd">
  <name>Task 1 (RED): 编写核心包 import 验证测试（先让测试失败）</name>
  <files>tests/__init__.py, tests/test_env_imports.py</files>
  <action>
    创建 tests/ 目录和 __init__.py（空文件）。

    创建 tests/test_env_imports.py，内容为对每个核心包的 import 测试，以及 .env.example 完整性测试：
    - test_import_anthropic: import anthropic，断言 hasattr(anthropic, 'Anthropic')
    - test_import_lancedb: import lancedb，断言模块可访问
    - test_import_watchdog: import watchdog.observers，断言模块可访问
    - test_import_paddleocr: import paddleocr，断言 hasattr(paddleocr, 'PaddleOCR')
    - test_import_sentence_transformers: from sentence_transformers import SentenceTransformer，断言类可访问
    - test_import_python_dotenv: from dotenv import load_dotenv，断言函数可调用
    - test_import_loguru: from loguru import logger，断言 logger 存在
    - test_no_agentscope: 尝试 import agentscope，预期抛出 ImportError（验证弃用包已移除）
    - test_env_example_exists_and_has_required_keys: 注意——config/.env.example 是棕地预存文件，
      本测试只验证其内容完整性，无需创建该文件。
      断言 config/.env.example 文件存在，并读取其内容，
      断言以下键名均出现在文件文本中（逐行检查键名前缀）：
        ANTHROPIC_API_KEY, LOG_LEVEL, LOG_DIR, LANCEDB_PATH, FEISHU_WEBHOOK_URL
      实现示例：
        ```python
        def test_env_example_exists_and_has_required_keys():
            env_example = Path("config/.env.example")
            assert env_example.exists(), "config/.env.example 不存在，OSS-03 要求提供示例配置文件"
            content = env_example.read_text(encoding="utf-8")
            required_keys = [
                "ANTHROPIC_API_KEY",
                "LOG_LEVEL",
                "LOG_DIR",
                "LANCEDB_PATH",
                "FEISHU_WEBHOOK_URL",
            ]
            for key in required_keys:
                assert key in content, f"config/.env.example 缺少必需键: {key}"
        ```
      注意：在文件顶部添加 from pathlib import Path。

    RED 步骤：运行 pytest tests/test_env_imports.py -v，预期部分测试失败（证明测试有效）。
    提交：git add tests/ && git commit -m "test(01-01): add failing import verification tests"
  </action>
  <verify>pytest tests/test_env_imports.py -v 输出显示测试被收集（即使失败），无语法错误</verify>
  <done>测试文件存在，pytest 可收集到 9 个测试函数，至少有 1 个因包未安装或 .env.example 缺失而失败（RED 状态确认）</done>
</task>

<task type="tdd">
  <name>Task 2 (GREEN): 修正 requirements.txt 并安装依赖使测试通过</name>
  <files>requirements.txt</files>
  <action>
    修改 requirements.txt 的两处问题：
    1. 删除 "agentscope>=0.1.0" 这一行（项目决策：不使用 AgentScope，改用 anthropic SDK 直接实现）
    2. 保留所有其他依赖不变

    修正后的 requirements.txt 核心依赖块：
    ```
    # 核心依赖
    anthropic>=0.40.0

    # 文件监控
    watchdog>=4.0.0

    # OCR
    paddleocr>=2.7.0
    paddlepaddle>=2.6.0
    Pillow>=10.0.0

    # 向量数据库
    lancedb>=0.5.0
    sentence-transformers>=2.7.0

    # 邮件处理
    imaplib2>=3.6

    # HTTP/通知
    httpx>=0.27.0
    requests>=2.31.0

    # 数据处理
    numpy>=1.24.0
    pandas>=2.0.0

    # 工具库
    python-dotenv>=1.0.0
    pydantic>=2.0.0
    loguru>=0.7.0
    tqdm>=4.66.0

    # 测试
    pytest>=7.4.0
    ```

    Windows 注意事项（记录在注释中）：
    - paddlepaddle 在 Windows 需要 Visual C++ Redistributable，若安装失败可用 paddlepaddle-gpu 替代或跳过 OCR 测试
    - paddleocr Windows 安装命令补充说明：pip install "paddleocr>=2.7.0" --no-deps 后手动安装依赖

    运行：pip install -r requirements.txt
    GREEN 步骤：运行 pytest tests/test_env_imports.py -v，预期所有测试通过。
    提交：git commit -m "feat(01-01): fix requirements.txt, remove agentscope, add pytest"
  </action>
  <verify>
    1. pip install -r requirements.txt 无 ERROR 输出（WARNING 可接受）
    2. pytest tests/test_env_imports.py -v 显示 9 passed（或 paddleocr 相关 skipped 并注明 Windows 兼容问题）
    3. python -c "import anthropic, lancedb, watchdog, loguru" 无报错
  </verify>
  <done>
    pytest tests/test_env_imports.py 全部通过（PASSED 或合理 SKIPPED）。
    requirements.txt 中不含 agentscope 字符串。
    test_no_agentscope 通过（确认 agentscope 未安装）。
    test_env_example_exists_and_has_required_keys 通过（确认 config/.env.example 存在且包含
    ANTHROPIC_API_KEY、LOG_LEVEL、LOG_DIR、LANCEDB_PATH、FEISHU_WEBHOOK_URL 五个键）。
  </done>
</task>

</tasks>

<verification>
全部验证命令：
1. grep -i "agentscope" requirements.txt — 预期无输出（已删除）
2. pip install -r requirements.txt — 预期无 ERROR
3. pytest tests/test_env_imports.py -v — 预期全部 PASSED（9 个测试）
4. python -c "import anthropic; print(anthropic.__version__)" — 输出版本号
</verification>

<success_criteria>
- requirements.txt 不含 agentscope
- requirements.txt 含 pytest>=7.4.0
- tests/test_env_imports.py 存在且包含 9 个测试函数
- pytest tests/test_env_imports.py 全部通过
- anthropic、lancedb、watchdog、sentence-transformers、python-dotenv、loguru 均可 import
- config/.env.example 存在且包含 ANTHROPIC_API_KEY、LOG_LEVEL、LOG_DIR、LANCEDB_PATH、FEISHU_WEBHOOK_URL 五个键
</success_criteria>

<output>
完成后创建 .planning/phases/01-基础环境/01-01-SUMMARY.md，记录：
- requirements.txt 的修改内容（删除 agentscope，添加 pytest）
- Windows 环境下 paddleocr 安装的注意事项
- 测试套件结构和通过状态
</output>
