---
phase: 01-基础环境
plan: "02"
type: tdd
wave: 2
depends_on:
  - "01-01"
files_modified:
  - config/load_config.py
  - scripts/init_lancedb.py
  - tests/test_config_loader.py
  - tests/test_lancedb_init.py
autonomous: true
requirements:
  - MONITOR-03
  - RETRIEVAL-03

must_haves:
  truths:
    - "配置加载脚本读取 .env 文件后，所有必需环境变量均可访问且无 KeyError"
    - "LanceDB 初始化脚本从 sample-cases.json 导入 8 条案例后，命令行输出'案例库初始化成功，共 8 条记录'"
    - "logs/ 目录自动创建，监控日志文件可写入"
    - "monitor/wechat_monitor.py 包含去重机制代码（_processed_files 或 cooldown），存在性验证通过（MONITOR-03 基础设施；运行时行为在 Phase 2 验证）"
    - "pytest tests/ 共 13 个测试全部通过（9 import + 5 config/dedup + 4 lancedb）"
  artifacts:
    - path: "config/load_config.py"
      provides: "配置加载函数，读取 .env 并验证必需变量"
      exports: ["load_config", "AppConfig"]
    - path: "scripts/init_lancedb.py"
      provides: "LanceDB 案例库初始化脚本"
      exports: ["init_case_database"]
    - path: "tests/test_config_loader.py"
      provides: "配置加载的 TDD 测试套件"
      exports: ["test_load_config_success", "test_missing_required_key_raises", "test_logs_dir_created"]
    - path: "tests/test_lancedb_init.py"
      provides: "LanceDB 初始化的 TDD 测试套件"
      exports: ["test_init_from_sample_cases", "test_output_message_format", "test_case_count"]
  key_links:
    - from: "config/load_config.py"
      to: "config/.env.example"
      via: "python-dotenv load_dotenv"
      pattern: "load_dotenv.*ANTHROPIC_API_KEY|os\\.environ\\.get"
    - from: "scripts/init_lancedb.py"
      to: "utils/lancedb_client.py"
      via: "LanceDBClient.add_case()"
      pattern: "LanceDBClient|add_case"
    - from: "scripts/init_lancedb.py"
      to: "cases/sample-cases.json"
      via: "json.load()"
      pattern: "sample-cases\\.json"
---

<objective>
用 TDD 方式编写配置加载脚本和 LanceDB 初始化脚本，验证 Phase 1 的核心功能需求。

Purpose: 证明系统在 Windows 11 上可正确读取配置、初始化案例库、自动创建日志目录——这是后续所有 Agent 运行的基础。
Output: config/load_config.py + scripts/init_lancedb.py + 覆盖两者的 TDD 测试套件。
</objective>

<execution_context>
@C:/Users/EDY/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/EDY/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/个人项目/mutiagent_trea/.planning/PROJECT.md
@D:/个人项目/mutiagent_trea/.planning/ROADMAP.md
@D:/个人项目/mutiagent_trea/.planning/STATE.md
@D:/个人项目/mutiagent_trea/config/.env.example
@D:/个人项目/mutiagent_trea/utils/lancedb_client.py
@D:/个人项目/mutiagent_trea/cases/sample-cases.json
@D:/个人项目/mutiagent_trea/.planning/phases/01-基础环境/01-01-SUMMARY.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1 (RED): 编写配置加载和日志目录的失败测试</name>
  <files>tests/test_config_loader.py</files>
  <action>
    创建 tests/test_config_loader.py，测试尚不存在的 config/load_config.py 模块。

    测试内容：
    1. test_load_config_returns_required_keys：
       - 在临时目录创建包含所有必需键的 .env 文件（ANTHROPIC_API_KEY、LOG_LEVEL、LOG_DIR、LANCEDB_PATH、EMBEDDING_MODEL）
       - 调用 load_config(env_path=tmp_env) 并断言返回对象包含这些属性且值非空

    2. test_missing_anthropic_key_raises_error：
       - 创建缺少 ANTHROPIC_API_KEY 的 .env 文件
       - 调用 load_config() 预期抛出 ValueError 或 KeyError，错误信息包含 "ANTHROPIC_API_KEY"

    3. test_logs_dir_auto_created：
       - 调用 load_config() 时传入不存在的 LOG_DIR 路径（临时目录下的 logs 子目录）
       - 断言函数返回后该目录已自动创建（Path(config.log_dir).exists() is True）

    4. test_config_reads_env_example_keys：
       - 使用真实的 config/.env.example 文件（但替换 ANTHROPIC_API_KEY 为 test_key）
       - 验证所有来自 .env.example 的键都被正确加载

    使用 pytest tmp_path fixture 隔离测试环境，不污染真实 .env 文件。

    5. test_monitor_dedup_infrastructure_exists（MONITOR-03 基础设施验证）：
       - 这是棕地项目，去重逻辑已在 monitor/wechat_monitor.py 中实现
       - 测试只做代码存在性检查（不运行监控器）：
         ```python
         def test_monitor_dedup_infrastructure_exists():
             source = Path("monitor/wechat_monitor.py").read_text(encoding="utf-8")
             assert "_processed_files" in source or "cooldown" in source, \
                 "monitor/wechat_monitor.py 中未找到去重机制（_processed_files 或 cooldown）"
             assert "logs" in source or "logger" in source, \
                 "monitor/wechat_monitor.py 中未找到日志记录逻辑"
         ```
       - 注意：此测试验证去重基础设施存在，完整行为测试在 Phase 2 进行

    RED 步骤：运行 pytest tests/test_config_loader.py -v
    预期：ModuleNotFoundError: No module named 'config.load_config'（证明测试有效）
    提交：git add tests/test_config_loader.py && git commit -m "test(01-02): add failing config loader and dedup infra tests"
  </action>
  <verify>pytest tests/test_config_loader.py -v 输出显示 ERROR（ModuleNotFoundError），说明测试到达了 import 阶段（RED 确认）</verify>
  <done>5 个测试函数被 pytest 收集（4 config + 1 dedup存在性），因 config.load_config 模块不存在而失败——RED 状态确认；test_monitor_dedup_infrastructure_exists 应立即通过（棕地代码已存在）</done>
</task>

<task type="tdd">
  <name>Task 2 (RED): 编写 LanceDB 初始化的失败测试</name>
  <files>tests/test_lancedb_init.py</files>
  <action>
    创建 tests/test_lancedb_init.py，测试尚不存在的 scripts/init_lancedb.py 模块。

    测试内容：
    1. test_init_loads_all_sample_cases（使用 tmp_path 隔离 LanceDB）：
       - 调用 init_case_database(db_path=tmp_path/"lancedb", cases_path="cases/sample-cases.json")
       - 断言返回值为整数 8（sample-cases.json 共 8 条）

    2. test_output_contains_success_message（捕获 stdout）：
       - 使用 capsys 捕获标准输出
       - 调用 init_case_database(...)
       - 断言输出包含"案例库初始化成功" 和 "8" 字样

    3. test_idempotent_init（幂等性）：
       - 对同一 db_path 调用两次 init_case_database
       - 断言第二次不重复插入，返回仍为 8（而非 16）
       - 断言第二次输出包含"已存在"或"跳过"字样

    4. test_missing_cases_file_raises：
       - 传入不存在的 cases_path
       - 预期抛出 FileNotFoundError

    RED 步骤：运行 pytest tests/test_lancedb_init.py -v
    预期：ModuleNotFoundError（scripts.init_lancedb 不存在）
    提交：git add tests/test_lancedb_init.py && git commit -m "test(01-02): add failing lancedb init tests"
  </action>
  <verify>pytest tests/test_lancedb_init.py -v 显示 ERROR（ModuleNotFoundError）——RED 状态确认</verify>
  <done>4 个测试函数被收集，因 scripts.init_lancedb 不存在而失败——RED 状态确认</done>
</task>

<task type="tdd">
  <name>Task 3 (GREEN): 实现 config/load_config.py 和 scripts/init_lancedb.py 使测试通过</name>
  <files>config/load_config.py, scripts/__init__.py, scripts/init_lancedb.py</files>
  <action>
    实现 config/load_config.py：

    ```python
    # config/load_config.py
    """配置加载模块——读取 .env 文件，验证必需变量，自动创建 logs/ 目录"""
    import os
    from dataclasses import dataclass
    from pathlib import Path
    from dotenv import load_dotenv

    REQUIRED_KEYS = ["ANTHROPIC_API_KEY"]

    @dataclass
    class AppConfig:
        anthropic_api_key: str
        log_level: str
        log_dir: str
        lancedb_path: str
        embedding_model: str
        rag_top_k: int
        rag_similarity_threshold: float

    def load_config(env_path: str | None = None) -> AppConfig:
        """
        加载 .env 配置文件，验证必需变量，自动创建 log_dir。
        Args:
            env_path: .env 文件路径，默认加载当前工作目录下的 .env
        Returns:
            AppConfig 实例
        Raises:
            ValueError: 若 ANTHROPIC_API_KEY 等必需变量缺失
        """
        if env_path:
            load_dotenv(env_path, override=True)
        else:
            load_dotenv(override=True)

        for key in REQUIRED_KEYS:
            if not os.environ.get(key):
                raise ValueError(f"必需的环境变量 {key!r} 未设置，请检查 .env 文件")

        log_dir = os.environ.get("LOG_DIR", "logs/")
        Path(log_dir).mkdir(parents=True, exist_ok=True)

        return AppConfig(
            anthropic_api_key=os.environ["ANTHROPIC_API_KEY"],
            log_level=os.environ.get("LOG_LEVEL", "INFO"),
            log_dir=log_dir,
            lancedb_path=os.environ.get("LANCEDB_PATH", "cases/lancedb"),
            embedding_model=os.environ.get(
                "EMBEDDING_MODEL",
                "sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2"
            ),
            rag_top_k=int(os.environ.get("RAG_TOP_K", "5")),
            rag_similarity_threshold=float(
                os.environ.get("RAG_SIMILARITY_THRESHOLD", "0.65")
            ),
        )
    ```

    实现 scripts/__init__.py（空文件）和 scripts/init_lancedb.py：

    ```python
    # scripts/init_lancedb.py
    """LanceDB 案例库初始化脚本——从 sample-cases.json 导入所有案例"""
    import json
    import sys
    from pathlib import Path

    def init_case_database(
        db_path: str = "cases/lancedb",
        cases_path: str = "cases/sample-cases.json",
    ) -> int:
        """
        从 JSON 文件初始化 LanceDB 案例库。
        幂等：若案例已存在则跳过重复插入。

        Args:
            db_path: LanceDB 存储路径
            cases_path: sample-cases.json 路径

        Returns:
            实际案例总数（不含占位记录）

        Raises:
            FileNotFoundError: 若 cases_path 不存在
        """
        cases_file = Path(cases_path)
        if not cases_file.exists():
            raise FileNotFoundError(f"案例文件不存在: {cases_path}")

        # 导入放在函数内部，避免 import 时触发模型加载
        from utils.lancedb_client import LanceDBClient

        client = LanceDBClient(db_path=db_path)
        client.init_schema()

        current_count = client.count()
        if current_count > 0:
            print(f"案例库已存在，共 {current_count} 条记录，跳过初始化")
            return current_count

        with open(cases_file, encoding="utf-8") as f:
            cases = json.load(f)

        for case in cases:
            client.add_case(case)

        total = client.count()
        print(f"案例库初始化成功，共 {total} 条记录")
        return total


    if __name__ == "__main__":
        count = init_case_database()
        sys.exit(0 if count > 0 else 1)
    ```

    GREEN 步骤：运行：
    1. pytest tests/test_config_loader.py -v
    2. pytest tests/test_lancedb_init.py -v

    若 paddleocr/sentence-transformers 首次运行需下载模型，使用 --timeout=300 参数。

    REFACTOR（若需要）：检查 load_config.py 中是否有重复的环境变量读取，提取为辅助函数。

    提交：git commit -m "feat(01-02): implement config loader and lancedb init scripts"
  </action>
  <verify>
    1. pytest tests/test_config_loader.py -v — 4 passed
    2. pytest tests/test_lancedb_init.py -v — 4 passed
    3. python scripts/init_lancedb.py — 输出"案例库初始化成功，共 8 条记录"
    4. python -c "from config.load_config import load_config" — 无报错
    5. ls logs/ — logs 目录存在（由测试或手动调用 load_config 创建）
  </verify>
  <done>
    - pytest tests/ -v 全部通过（含 test_env_imports.py + test_config_loader.py + test_lancedb_init.py，共 12 个测试：8 import + 4 config + 4 lancedb）
    - python scripts/init_lancedb.py 输出包含"案例库初始化成功，共 8 条记录"
    - config/load_config.py 和 scripts/init_lancedb.py 文件存在
    - logs/ 目录存在且可写
  </done>
</task>

</tasks>

<verification>
Phase 1 整体验证（按 ROADMAP Success Criteria 逐条）：

1. SC-1（依赖安装）：pytest tests/test_env_imports.py -v — 全部 PASSED
2. SC-2（配置加载）：pytest tests/test_config_loader.py -v — 全部 PASSED
3. SC-3（LanceDB 初始化）：python scripts/init_lancedb.py — 输出"案例库初始化成功，共 8 条记录"
4. SC-4（logs 目录）：python -c "from config.load_config import load_config; import os; os.environ.setdefault('ANTHROPIC_API_KEY','test'); load_config()" && ls logs/

运行全套测试：pytest tests/ -v --tb=short
</verification>

<success_criteria>
- config/load_config.py 存在，load_config() 可调用，缺少必需变量时抛出 ValueError
- scripts/init_lancedb.py 存在，从 sample-cases.json 导入 8 条案例后输出正确消息
- logs/ 目录在 load_config() 调用后自动创建
- pytest tests/ 全部通过（12 个测试：8 import + 4 config + 4 lancedb）
- 初始化幂等：重复运行 init_lancedb.py 不导致重复插入
</success_criteria>

<output>
完成后创建 .planning/phases/01-基础环境/01-02-SUMMARY.md，记录：
- config/load_config.py 的 AppConfig 结构和必需变量列表
- scripts/init_lancedb.py 的幂等初始化机制
- LanceDB 首次运行 sentence-transformers 模型下载耗时（记录实际时间）
- pytest 最终通过截图/输出
</output>
