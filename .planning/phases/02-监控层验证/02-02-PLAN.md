---
phase: 02-监控层验证
plan: "02"
type: tdd
wave: 2
depends_on:
  - "02-01"
files_modified:
  - tests/test_email_monitor.py
autonomous: true
requirements:
  - MONITOR-02

must_haves:
  truths:
    - "EmailMonitor._fetch_new_emails() 在有未读邮件时返回包含 subject/sender/attachments 的 dict 列表"
    - "EmailMonitor._parse_email() 正确解码 RFC 2047 中文主题，提取图片附件并保存到 attachment_save_dir"
    - "EmailMonitor._create_task() 调用 task_callback 且 task_input 含 has_images=True（当有图片附件时）"
    - "EmailMonitor 连接失败时 start() 返回 False，logger.error 被调用"
    - "IMAP mock 模式：所有核心逻辑测试不依赖真实邮件服务器"
    - "Playwright 备路测试：标记 pytest.mark.playwright，验证从浏览器抓取邮件后的处理逻辑"
  artifacts:
    - path: "tests/test_email_monitor.py"
      provides: "邮件监控全套 TDD 测试（IMAP 主路 mock + Playwright 备路）"
      min_lines: 150
      contains: "MagicMock"
  key_links:
    - from: "tests/test_email_monitor.py"
      to: "monitor/email_monitor.py"
      via: "import EmailMonitor"
      pattern: "from monitor.email_monitor import"
    - from: "EmailMonitor._parse_email"
      to: "attachment_save_dir"
      via: "open(save_path, 'wb').write(payload)"
      pattern: "attachment_save_dir"
    - from: "EmailMonitor._create_task"
      to: "task_callback"
      via: "self.task_callback(task_input)"
      pattern: "task_callback"
---

<objective>
为已有的 monitor/email_monitor.py 编写完整 TDD 测试套件，覆盖：IMAP 轮询检测（MONITOR-02）的主路（mock IMAP）和备路（Playwright 结构占位）。

Purpose: 证明邮件监控层能正确解析邮件、提取图片附件并触发分析任务，在不依赖真实邮件账号的情况下通过 mock 验证全部核心路径。
Output: tests/test_email_monitor.py，包含 IMAP mock 单元测试和 Playwright 备路骨架，全部非 skip 测试通过。
</objective>

<execution_context>
@C:/Users/EDY/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/EDY/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@monitor/email_monitor.py
@.planning/phases/02-监控层验证/02-01-SUMMARY.md
</context>

<feature>
  <name>Email 监控 TDD 测试套件（IMAP 主路 + Playwright 备路）</name>
  <files>tests/test_email_monitor.py, monitor/email_monitor.py</files>
  <behavior>
    测试分三组：

    【IMAP 主路单元测试组 — mock imaplib.IMAP4_SSL】
    - _connect() 登录成功 → 返回 True，logger.info 包含 '已连接'
    - _connect() 抛出 Exception → 返回 False，logger.error 包含 '连接失败'
    - _fetch_new_emails() 无未读 → 返回 []
    - _fetch_new_emails() 有 1 封未读含图片附件 → 返回长度 1 的列表，attachments 非空，is_image=True
    - _fetch_new_emails() 有 2 封未读 → 返回长度 2 的列表
    - _parse_email() RFC 2047 编码中文主题 → subject 解码正确（非乱码）
    - _parse_email() 含 image/jpeg 附件 → attachments[0]['is_image'] == True，文件已写入 attachment_save_dir
    - _parse_email() 纯文本邮件无附件 → attachments == []，body_text 非空
    - _create_task() → task_callback 被调用 1 次，task_input['source'] == 'email'
    - _create_task() 含图片附件 → task_input['has_images'] == True
    - start() 当 _connect() 返回 False → 返回 False（不进入轮询循环）

    【IMAP 主路集成测试组 — 构造真实 email.message.Message 对象】
    使用 email 标准库构造完整 RFC822 邮件（含 MIME 附件），直接调用 _parse_email()，不 mock。
    - 构造含 image/png 附件的 MIME 邮件 → parse_email 返回 attachments，文件写入 tmp_path
    - 构造 GB2312 编码主题 '测试邮件' → subject 解码为中文
    - 构造 multipart 邮件含 text/plain + image/jpeg → body_text 和 attachments 均正确提取

    【Playwright 备路测试组 — 骨架测试，pytest.mark.playwright 标记】
    注意：Playwright 备路在本 Phase 2 中为结构占位。
    实际 Playwright MCP 调用在执行阶段由 Claude 实现，这里只建立测试骨架。
    - test_playwright_path_structure：验证如果提供一个包含邮件列表的 dict（模拟从浏览器抓取的数据），
      EmailMonitor._create_task() 能正确处理并调用 callback。
      （此测试不依赖 Playwright，只验证数据处理逻辑）
    - 测试标记 @pytest.mark.playwright，在 pytest.ini/conftest 中注册为自定义 mark

    Cases（_fetch_new_emails 单元测试）:
      mock search 返回 b'' → return []
      mock search 返回 b'1' → return [parsed_email_dict]
      mock fetch 抛异常 → logger.warning 调用，该邮件跳过，其他正常
  </behavior>
  <implementation>
    brownfield 模式：monitor/email_monitor.py 已实现，测试只验证现有行为。

    关键 mock 策略：
    1. mock imaplib.IMAP4_SSL：
       ```python
       with patch('monitor.email_monitor.imaplib.IMAP4_SSL') as MockIMAP:
           mock_conn = MagicMock()
           MockIMAP.return_value = mock_conn
           mock_conn.login.return_value = ('OK', [b'Logged in'])
           mock_conn.select.return_value = ('OK', [b'1'])
           mock_conn.search.return_value = ('OK', [b'1'])
           mock_conn.fetch.return_value = ('OK', [(b'1 (RFC822 {123})', raw_email_bytes)])
       ```

    2. 构造 RFC822 邮件字节：
       ```python
       import email.mime.multipart, email.mime.text, email.mime.image
       msg = email.mime.multipart.MIMEMultipart()
       msg['Subject'] = '测试'
       msg['From'] = 'test@example.com'
       msg['Date'] = 'Mon, 14 Mar 2026 10:00:00 +0800'
       msg.attach(email.mime.text.MIMEText('测试正文', 'plain', 'utf-8'))
       # 添加图片附件
       img_part = email.mime.image.MIMEImage(b'\x89PNG...', _subtype='png')
       img_part.add_header('Content-Disposition', 'attachment', filename='test.png')
       msg.attach(img_part)
       raw_bytes = msg.as_bytes()
       ```

    3. RFC 2047 编码主题测试：
       ```python
       from email.header import make_header, decode_header
       encoded = '=?utf-8?b?5rWL6K+V6YKu5Lu2?='  # '测试邮件' base64
       ```

    4. EmailMonitor fixture：
       ```python
       @pytest.fixture
       def monitor(tmp_path):
           return EmailMonitor(
               host='imap.test.com', port=993,
               username='test@test.com', password='pass',
               task_callback=MagicMock(),
               pipeline_dir=str(tmp_path / '.pipeline/tasks'),
               attachment_save_dir=str(tmp_path / 'attachments')
           )
       ```

    5. conftest.py 中注册 playwright mark：
       如果 tests/conftest.py 已存在，追加 pytest_configure 中注册；
       如不存在，在 tests/ 创建 conftest.py 注册 'playwright' mark。

    Playwright 备路说明：
    用户决策：Playwright 为备路（IMAP 为主路）。
    本测试中 Playwright 备路实现为：验证如果从外部（如 Playwright 抓取）得到的邮件数据 dict
    传入 _create_task()，处理逻辑是否正确。不需要真实启动浏览器。
    真实 Playwright MCP 集成留待后续阶段。
  </implementation>
</feature>

<tasks>

<task type="tdd">
  <name>Task 1: RED — 编写 EmailMonitor 单元测试（mock IMAP + 真实 MIME 构造）</name>
  <files>tests/test_email_monitor.py</files>
  <action>
    在 tests/test_email_monitor.py 创建完整测试文件。

    文件顶部导入：
    ```python
    import sys, email, email.mime.multipart, email.mime.text, email.mime.image
    from pathlib import Path
    from unittest.mock import MagicMock, patch
    import pytest
    from monitor.email_monitor import EmailMonitor
    ```

    Fixture monitor(tmp_path)：创建 EmailMonitor 实例，pipeline_dir 和 attachment_save_dir 都在 tmp_path 内。

    测试类 TestEmailMonitorConnect（mock IMAP4_SSL）：
    - test_connect_success：patch IMAP4_SSL，login 成功 → _connect() 返回 True，logger.info 含 '已连接'
    - test_connect_failure：patch IMAP4_SSL，login 抛 Exception → _connect() 返回 False，logger.error 含 '连接失败'

    测试类 TestFetchNewEmails（mock IMAP4_SSL connection）：
    构造辅助函数 make_raw_email(subject, from_addr, has_image=False) → bytes，
    使用 email.mime 构造真实 RFC822 字节。

    - test_no_unread_returns_empty：mock search 返回 b'' → return []
    - test_one_unread_text_only：mock search 返回 b'1'，fetch 返回纯文本邮件 bytes → 返回列表长度 1，attachments=[]
    - test_one_unread_with_image：mock 返回含 image/png 附件邮件 → attachments[0]['is_image'] == True
    - test_two_unread：mock search 返回 b'1 2' → 返回列表长度 2
    - test_fetch_parse_error_skips：mock fetch 抛 Exception → logger.warning 调用，返回 []（跳过该邮件）

    测试类 TestParseEmail（直接调用 _parse_email，不 mock imaplib）：
    - test_parse_chinese_subject_utf8：构造 UTF-8 base64 编码主题 → subject 解码为中文
    - test_parse_image_attachment_saved：构造含 image/png 附件邮件 → attachments 非空，文件存在于 attachment_save_dir
    - test_parse_text_only：纯文本邮件 → body_text 非空，attachments == []
    - test_parse_gb2312_subject：构造 GB2312 编码主题（=?gb2312?b?...?=）→ 正确解码

    测试类 TestCreateTask：
    - test_create_task_calls_callback：传入 email_data → task_callback.call_count == 1
    - test_create_task_has_images_true：email_data 含 image 附件 → task_input['has_images'] == True
    - test_create_task_has_images_false：无附件 → task_input['has_images'] == False
    - test_create_task_writes_input_json：pipeline_dir 内出现 email-*/input.json

    测试类 TestEmailMonitorStart：
    - test_start_connect_failure_returns_false：patch _connect 返回 False → start() 返回 False

    测试类 TestPlaywrightPath（@pytest.mark.playwright）：
    - test_playwright_data_processing：构造模拟 Playwright 抓取的邮件 dict（含 subject/sender/attachments），
      直接调用 monitor._create_task(email_data)，断言 task_callback 被调用，
      task_input['source'] == 'email'（验证数据处理逻辑与抓取方式无关）

    同时检查 tests/conftest.py 是否存在：
    - 若存在，追加注册 playwright mark 的代码（不覆盖已有内容）
    - 若不存在，创建 tests/conftest.py 注册 playwright mark
  </action>
  <verify>
    cd "D:/个人项目/mutiagent_trea" && python -m pytest tests/test_email_monitor.py -v --tb=short 2>&1 | head -80
  </verify>
  <done>
    tests/test_email_monitor.py 存在，行数 >= 150 行。
    pytest collect 阶段无 ERROR（import error 等）。
    至少有 15 个测试被收集。
  </done>
</task>

<task type="tdd">
  <name>Task 2: GREEN — 运行测试，修正断言细节，确保所有非 skip 测试通过</name>
  <files>tests/test_email_monitor.py</files>
  <action>
    brownfield 模式：monitor/email_monitor.py 已实现，不修改源码。

    运行测试，定位 FAILED，在测试文件内修正断言。常见需要修正的点：

    1. _fetch_new_emails 中 ids = message_ids[0].split()
       mock search 返回值格式：mock_conn.search.return_value = ('OK', [b'1 2'])
       ids 会是 [b'1', b'2']，fetch 需要分别被调用。
       mock_conn.fetch.side_effect = [fetch_result_1, fetch_result_2]

    2. _parse_email 中附件判断条件：
       "attachment" in content_disposition OR content_type.startswith("image/")
       构造测试邮件时确保 Content-Disposition 包含 'attachment'。

    3. 附件文件路径：save_path = attachment_save_dir / f"{msg_id}_{decoded_name}"
       断言文件存在时用 monitor.attachment_save_dir / f"1_test.png"（msg_id 为 "1"）。

    4. _connect 的 logger patch：路径为 'monitor.email_monitor.logger'

    5. _create_task 中 task_input['has_images'] 依赖 any(a['is_image'] for a in attachments)
       构造 email_data 时 attachments 需包含 {'is_image': True, ...} 结构。

    6. start() 返回 False 测试：patch monitor._connect 为 MagicMock(return_value=False)，
       直接调用 monitor.start()，但 start() 内有 while self._running 循环。
       _connect 返回 False 时 start() 直接 return False，不进入循环，安全。

    运行修正循环：
    python -m pytest tests/test_email_monitor.py -v --tb=long
    逐条修正直至全绿。

    最终验证：
    python -m pytest tests/test_wechat_monitor.py tests/test_email_monitor.py -v --tb=short
    两个测试文件一起运行，确认无相互干扰。
  </action>
  <verify>
    cd "D:/个人项目/mutiagent_trea" && python -m pytest tests/test_email_monitor.py -v 2>&1 && python -m pytest tests/test_wechat_monitor.py tests/test_email_monitor.py --tb=short 2>&1 | tail -20
  </verify>
  <done>
    python -m pytest tests/test_email_monitor.py -v 输出中：
    - 所有非 skip 测试状态为 PASSED
    - @pytest.mark.playwright 测试通过（不依赖真实浏览器）
    - 无任何 FAILED 或 ERROR
    - 测试总数 >= 15 个

    两文件联合运行：
    - tests/test_wechat_monitor.py 全部维持 PASSED
    - tests/test_email_monitor.py 全部 PASSED
    - 0 failed, 0 error
  </done>
</task>

</tasks>

<verification>
cd "D:/个人项目/mutiagent_trea" && python -m pytest tests/test_wechat_monitor.py tests/test_email_monitor.py -v --tb=short

期望输出（关键行）：
- PASSED: test_connect_success
- PASSED: test_connect_failure
- PASSED: test_no_unread_returns_empty
- PASSED: test_one_unread_with_image
- PASSED: test_parse_chinese_subject_utf8
- PASSED: test_parse_image_attachment_saved
- PASSED: test_create_task_calls_callback
- PASSED: test_create_task_has_images_true
- PASSED: test_playwright_data_processing
- 0 failed, 0 error
</verification>

<success_criteria>
1. tests/test_email_monitor.py 存在，行数 >= 150
2. IMAP 主路：所有 mock IMAP 单元测试 PASSED（MONITOR-02）
3. 真实 MIME 构造：_parse_email 集成测试 PASSED，中文主题正确解码，图片附件文件已写入
4. Playwright 备路骨架：@pytest.mark.playwright 测试 PASSED（不依赖真实浏览器）
5. 联合运行：两个测试文件合计 0 failed, 0 error
</success_criteria>

<output>
完成后创建 .planning/phases/02-监控层验证/02-02-SUMMARY.md，记录：
- 创建的测试文件路径
- 测试总数和通过数
- IMAP mock 策略的关键实现（imaplib patch 路径、RFC822 构造方式）
- Playwright 备路测试结构说明
- 任何 email_monitor.py 边界发现
</output>
