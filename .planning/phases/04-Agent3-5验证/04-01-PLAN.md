---
phase: 04-Agent3-5验证
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_agent3_discrimination.py
autonomous: true
requirements:
  - DISCRIM-01
  - DISCRIM-02

must_haves:
  truths:
    - "Agent3 接受 step1_result + step2_result，调用 Qwen3.5 后返回包含 fraud_probability、verdict、explanation 的字典"
    - "fraud_probability 在 0-1 之间的浮点数"
    - "verdict 只能是 诈骗/可疑/正常 之一"
    - "结果写入 step_dir/step3.json，文件格式合法"
    - "mock OpenAI 调用时，真实调用不发出，测试可离线运行"
    - "API 调用失败时，Agent3 优雅降级而非崩溃，status='failed'"
  artifacts:
    - path: "tests/test_agent3_discrimination.py"
      provides: "Agent3 TDD 测试套件"
      min_lines: 80
  key_links:
    - from: "tests/test_agent3_discrimination.py"
      to: "agents/agent3_discrimination.py"
      via: "unittest.mock.patch('openai.OpenAI')"
      pattern: "patch.*openai"
---

<objective>
为 DiscriminationAgent (Agent3) 编写 TDD 测试套件，验证其接收 Agent1+Agent2 的结构化输出、mock Qwen3.5 API（OpenAI SDK）并断言返回字段格式正确。

Purpose: 在不调用真实 Qwen API 的情况下，用 mock 验证 Agent3 的输入输出契约符合下游 Agent4 的期望。
Output: tests/test_agent3_discrimination.py（测试全部通过）
</objective>

<execution_context>
@C:/Users/EDY/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/EDY/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@agents/agent3_discrimination.py
@tests/test_agent2_retrieval.py
@tests/conftest.py
</context>

<tasks>

<task type="tdd">
  <name>Task 1: RED — 编写 Agent3 失败测试（输出格式 + mock openai）</name>
  <files>tests/test_agent3_discrimination.py</files>
  <action>
创建 tests/test_agent3_discrimination.py，写入以下测试。此时不修改任何 agent 代码，运行测试应确认测试文件可被 pytest 发现（已有 agent3 实现，这些测试可能直接通过，但必须先写测试确认接口契约）。

测试结构：

1. fixtures：
   - step_dir(tmp_path)：返回临时目录
   - step1_result：模拟 Agent1 输出，含 task_id="test-agent3-001"、intent_label="financial_fraud"、confidence=0.92、key_indicators=["转账","高回报"]、extracted_text_summary="..."、extracted_text="转账给我，可获高额回报"
   - step2_result：模拟 Agent2 输出，含 task_id="test-agent3-001"、avg_similarity=0.85、low_similarity_warning=False、relevant_cases=[{id:"case-001",similarity:0.85,description:"...",verdict:"诈骗"}]

2. mock 辅助 fixture agent_with_mock：
   - 使用 unittest.mock.patch("agents.agent3_discrimination.OpenAI") 打补丁
   - 模拟 client.chat.completions.create 返回结构：
     response.choices[0].message.content = json.dumps({"fraud_probability":0.92,"verdict":"诈骗","fraud_type":"金融诈骗","evidence":["要求转账","高回报承诺"],"counter_evidence":[],"explanation":"明显诈骗特征","case_reference_ids":["case-001"]})
   - yield DiscriminationAgent() 实例（此时 OpenAI() 已被 mock）

3. 测试函数：

test_returns_dict_with_required_fields(agent_with_mock, step1_result, step2_result, step_dir)：
  - result = agent.run(step1_result, step2_result, step_dir)
  - 断言 result 为 dict
  - 断言 result["step"] == 3
  - 断言 result["agent"] == "discrimination"
  - 断言 result["status"] == "success"

test_fraud_probability_in_range(agent_with_mock, step1_result, step2_result, step_dir)：
  - result = agent.run(step1_result, step2_result, step_dir)
  - 断言 "fraud_probability" in result
  - 断言 isinstance(result["fraud_probability"], float)
  - 断言 0.0 <= result["fraud_probability"] <= 1.0

test_verdict_is_valid_value(agent_with_mock, step1_result, step2_result, step_dir)：
  - result = agent.run(step1_result, step2_result, step_dir)
  - VALID_VERDICTS = {"诈骗", "可疑", "正常"}
  - 断言 result["verdict"] in VALID_VERDICTS，错误信息含实际值

test_explanation_is_non_empty_string(agent_with_mock, step1_result, step2_result, step_dir)：
  - result = agent.run(step1_result, step2_result, step_dir)
  - 断言 "explanation" in result
  - 断言 isinstance(result["explanation"], str)
  - 断言 len(result["explanation"]) > 0

test_step3_json_written_correctly(agent_with_mock, step1_result, step2_result, step_dir)：
  - agent.run(step1_result, step2_result, step_dir)
  - step_file = step_dir / "step3.json"
  - 断言 step_file.exists()
  - data = json.load(step_file.open())
  - 断言 data["step"] == 3, data["agent"] == "discrimination"

test_evidence_is_list(agent_with_mock, step1_result, step2_result, step_dir)：
  - result = agent.run(step1_result, step2_result, step_dir)
  - 断言 isinstance(result["evidence"], list)

test_graceful_failure_on_api_error(step1_result, step2_result, step_dir)：
  - 使用 patch("agents.agent3_discrimination.OpenAI") 让 client.chat.completions.create 抛出 Exception("API error")
  - result = DiscriminationAgent().run(step1_result, step2_result, step_dir)
  - 断言 result["status"] == "failed"
  - 断言 "error" in result
  - 断言 result.get("fraud_probability") == 0.5（降级值）

test_task_id_propagated(agent_with_mock, step1_result, step2_result, step_dir)：
  - result = agent.run(step1_result, step2_result, step_dir)
  - 断言 result["task_id"] == step1_result["task_id"]

注意：fixture agent_with_mock 需要用 @pytest.fixture 装饰，并在 with patch(...) as mock_anthropic: 块内设置 mock_instance.messages.create.return_value，MagicMock content[0].text 赋值。
  </action>
  <verify>cd D:/个人项目/mutiagent_trea && python -m pytest tests/test_agent3_discrimination.py -v 2>&amp;1 | head -50</verify>
  <done>pytest 收集到 8 个测试，无 ImportError，全部通过（PASSED）</done>
</task>

<task type="tdd">
  <name>Task 2: GREEN — 验证实现通过所有 Agent3 测试（含 json 代码块解析路径）</name>
  <files>tests/test_agent3_discrimination.py</files>
  <action>
运行完整测试套件。如果存在测试失败，分析原因并修正测试（而非修改 agent 代码）：

1. 若 json 解析路径问题：agent3 的 ```json 代码块剥离逻辑在 mock 场景下可能不触发。检查 mock 返回的 text 是否需要包裹在 ```json\n...\n``` 中，调整 mock fixture 的 return 值。

2. 若 fixture 作用域问题：step_dir 使用 tmp_path（function 作用域），不与 module 作用域 fixture 混用。

3. 若 MagicMock 的 content[0].text 不可访问：改用显式 MagicMock 对象：
   mock_content = MagicMock()
   mock_content.text = json.dumps({...})
   mock_response = MagicMock()
   mock_response.content = [mock_content]
   mock_instance.messages.create.return_value = mock_response

运行：python -m pytest tests/test_agent3_discrimination.py -v --tb=short

确认所有 8 个测试 PASSED 后提交。
  </action>
  <verify>cd D:/个人项目/mutiagent_trea && python -m pytest tests/test_agent3_discrimination.py -v --tb=short 2>&amp;1</verify>
  <done>pytest 输出 "8 passed"，无 FAILED、ERROR 或 WARNING（PytestUnknownMarkWarning 除外）</done>
</task>

</tasks>

<verification>
python -m pytest tests/test_agent3_discrimination.py -v --tb=short
预期：8 passed, 0 failed
</verification>

<success_criteria>
1. tests/test_agent3_discrimination.py 文件存在，含 8 个测试函数
2. 所有测试不依赖真实 Anthropic API（全部 mock）
3. 测试覆盖：输出字段格式、fraud_probability 范围、verdict 合法值、explanation 非空、step3.json 写入、降级失败处理
4. pytest 全部通过，无 import 错误
</success_criteria>

<output>
完成后创建 .planning/phases/04-Agent3-5验证/04-01-SUMMARY.md
</output>
