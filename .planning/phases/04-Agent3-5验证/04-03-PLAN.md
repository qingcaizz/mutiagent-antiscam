---
phase: 04-Agent3-5验证
plan: "03"
type: tdd
wave: 2
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - tests/test_agent5_intervention.py
autonomous: true
requirements:
  - INTERV-01
  - INTERV-02
  - INTERV-03
  - INTERV-04

must_haves:
  truths:
    - "中风险及以上时，Agent5 尝试向飞书/钉钉 Webhook 发送请求（mock 验证调用次数）"
    - "安全/低风险时，Agent5 不发送 Webhook 请求"
    - "每次运行在 reports/YYYY-MM-DD/{task_id}.md 生成 Markdown 报告文件"
    - "requires_guardian_alert=True 时，_alert_guardian 被调用（GUARDIAN_FEISHU 环境变量控制）"
    - "result 包含 awaiting_feedback: True（INTERV-04 反馈入口标志）"
    - "step5.json 写入 step_dir，格式合法"
    - "低风险时不推送 Webhook，report 仍然生成"
  artifacts:
    - path: "tests/test_agent5_intervention.py"
      provides: "Agent5 TDD 测试套件，含 mock httpx 的 Webhook 调用验证"
      min_lines: 100
  key_links:
    - from: "tests/test_agent5_intervention.py"
      to: "utils/notifier.py"
      via: "unittest.mock.patch('httpx.AsyncClient')"
      pattern: "patch.*httpx"
    - from: "agents/agent5_intervention.py"
      to: "utils/notifier.py"
      via: "self.feishu.send(msg) / self.dingtalk.send(msg)"
      pattern: "feishu\\.send|dingtalk\\.send"
---

<objective>
为 InterventionAgent (Agent5) 编写 TDD 测试套件，使用 mock httpx 验证 Webhook 推送行为，验证报告文件生成和反馈入口标志。

Purpose: Agent5 的关键副作用（Webhook 推送、文件写入）需在不依赖真实飞书/钉钉账号的情况下验证，通过 mock httpx.AsyncClient 拦截 HTTP 调用。
Output: tests/test_agent5_intervention.py（测试全部通过）
</objective>

<execution_context>
@C:/Users/EDY/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/EDY/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@agents/agent5_intervention.py
@utils/notifier.py
@tests/test_agent2_retrieval.py
@tests/conftest.py
</context>

<tasks>

<task type="tdd">
  <name>Task 1: RED — 编写 Agent5 失败测试（mock httpx + 报告生成 + 反馈标志）</name>
  <files>tests/test_agent5_intervention.py</files>
  <action>
创建 tests/test_agent5_intervention.py。

关键 mock 策略：FeishuNotifier.send 和 DingTalkNotifier.send 内部通过 httpx.AsyncClient.post 发送请求。
最简单的 mock 路径是直接 patch utils.notifier.FeishuNotifier.send 和 utils.notifier.DingTalkNotifier.send（避开 asyncio 复杂性）。

测试结构：

1. 导入：
   import json, os, pytest
   from pathlib import Path
   from unittest.mock import patch, MagicMock
   from agents.agent5_intervention import InterventionAgent

2. fixtures：

@pytest.fixture
def reports_dir(tmp_path):
    d = tmp_path / "reports"
    d.mkdir()
    return d

@pytest.fixture
def conversations_dir(tmp_path):
    d = tmp_path / "conversations"
    d.mkdir()
    return d

@pytest.fixture
def step_dir(tmp_path):
    d = tmp_path / "steps"
    d.mkdir()
    return d

@pytest.fixture
def agent(reports_dir, conversations_dir):
    return InterventionAgent(
        reports_dir=str(reports_dir),
        conversations_dir=str(conversations_dir)
    )

@pytest.fixture
def step1_result():
    return {
        "task_id": "test-a5-001",
        "step": 1, "agent": "preprocessor", "status": "success",
        "intent_label": "financial_fraud", "confidence": 0.92,
        "key_indicators": ["转账", "高回报"],
        "extracted_text_summary": "用户收到诈骗信息",
        "extracted_text": "转账给我可获高额回报",
        "source": "wechat", "file_name": "test.png"
    }

@pytest.fixture
def step2_result():
    return {
        "task_id": "test-a5-001",
        "step": 2, "agent": "retrieval", "status": "success",
        "relevant_cases": [{"id":"case-001","similarity":0.85,"description":"诈骗案例","verdict":"诈骗"}],
        "avg_similarity": 0.85, "low_similarity_warning": False, "total_retrieved": 1
    }

@pytest.fixture
def step3_result():
    return {
        "task_id": "test-a5-001",
        "step": 3, "agent": "discrimination", "status": "success",
        "fraud_probability": 0.92, "verdict": "诈骗",
        "fraud_type": "金融诈骗",
        "evidence": ["要求转账", "高回报承诺"],
        "counter_evidence": [],
        "explanation": "明显诈骗特征，要求立即转账。"
    }

@pytest.fixture
def step4_high_risk():
    return {
        "task_id": "test-a5-001",
        "step": 4, "agent": "assessment", "status": "success",
        "base_score": 0.92, "final_risk_score": 0.92,
        "risk_level": "极高", "risk_level_key": "extreme_high",
        "requires_guardian_alert": True,
        "rule_adjustments": [], "total_adjustment": 0.0
    }

@pytest.fixture
def step4_medium_risk():
    return {
        "task_id": "test-a5-002",
        "step": 4, "agent": "assessment", "status": "success",
        "base_score": 0.60, "final_risk_score": 0.60,
        "risk_level": "中", "risk_level_key": "medium",
        "requires_guardian_alert": False,
        "rule_adjustments": [], "total_adjustment": 0.0
    }

@pytest.fixture
def step4_low_risk():
    return {
        "task_id": "test-a5-003",
        "step": 4, "agent": "assessment", "status": "success",
        "base_score": 0.20, "final_risk_score": 0.20,
        "risk_level": "安全", "risk_level_key": "safe",
        "requires_guardian_alert": False,
        "rule_adjustments": [], "total_adjustment": 0.0
    }

3. 测试函数：

test_returns_required_fields(agent, step1_result, step2_result, step3_result, step4_medium_risk, step_dir)：
   with patch("utils.notifier.FeishuNotifier.send", return_value=True):
       result = agent.run(step1_result, step2_result, step3_result, step4_medium_risk, step_dir)
   assert result["step"] == 5
   assert result["agent"] == "intervention"
   assert result["status"] == "success"
   assert "report_path" in result
   assert "awaiting_feedback" in result

test_awaiting_feedback_is_true(agent, step1_result, step2_result, step3_result, step4_medium_risk, step_dir)：
   （INTERV-04 验证）
   with patch("utils.notifier.FeishuNotifier.send", return_value=True):
       result = agent.run(step1_result, step2_result, step3_result, step4_medium_risk, step_dir)
   assert result["awaiting_feedback"] is True

test_report_generated_for_medium_risk(agent, step1_result, step2_result, step3_result, step4_medium_risk, step_dir, reports_dir)：
   （INTERV-02 验证）
   with patch("utils.notifier.FeishuNotifier.send", return_value=True):
       result = agent.run(step1_result, step2_result, step3_result, step4_medium_risk, step_dir)
   report_path = Path(result["report_path"])
   assert report_path.exists(), f"报告文件不存在: {report_path}"
   content = report_path.read_text(encoding="utf-8")
   assert "诈骗分析报告" in content
   assert step1_result["task_id"] in content

test_report_generated_for_low_risk(agent, step1_result, step2_result, step3_result, step4_low_risk, step_dir)：
   （低风险也生成报告，不推送通知）
   result = agent.run(step1_result, step2_result, step3_result, step4_low_risk, step_dir)
   assert result["status"] == "success"
   report_path = Path(result["report_path"])
   assert report_path.exists()

test_webhook_sent_for_medium_risk(agent, step1_result, step2_result, step3_result, step4_medium_risk, step_dir)：
   （INTERV-01 验证：中风险推送）
   with patch("utils.notifier.FeishuNotifier.send", return_value=True) as mock_feishu:
       # 设置 webhook_url 使其不为空
       agent.feishu.webhook_url = "https://mock.feishu.cn/hook/test"
       result = agent.run(step1_result, step2_result, step3_result, step4_medium_risk, step_dir)
   mock_feishu.assert_called_once()
   assert any(n["channel"] == "feishu" for n in result["notifications_sent"])

test_webhook_not_sent_for_low_risk(agent, step1_result, step2_result, step3_result, step4_low_risk, step_dir)：
   （低风险不推送）
   with patch("utils.notifier.FeishuNotifier.send", return_value=True) as mock_feishu:
       agent.feishu.webhook_url = "https://mock.feishu.cn/hook/test"
       result = agent.run(step1_result, step2_result, step3_result, step4_low_risk, step_dir)
   mock_feishu.assert_not_called()
   assert result["notifications_sent"] == []

test_guardian_alert_triggered_for_extreme_risk(agent, step1_result, step2_result, step3_result, step4_high_risk, step_dir)：
   （INTERV-03 验证）
   with patch.dict(os.environ, {"GUARDIAN_FEISHU": "https://mock.feishu.cn/guardian"}):
       with patch("utils.notifier.FeishuNotifier.send", return_value=True):
           result = agent.run(step1_result, step2_result, step3_result, step4_high_risk, step_dir)
   assert step4_high_risk["requires_guardian_alert"] is True
   # _alert_guardian 在有 GUARDIAN_FEISHU 时返回 True
   assert result.get("guardian_alerted") is True

test_guardian_not_alerted_without_env(agent, step1_result, step2_result, step3_result, step4_high_risk, step_dir)：
   （无 GUARDIAN_FEISHU 时不触发）
   env = {k: v for k, v in os.environ.items() if k != "GUARDIAN_FEISHU"}
   with patch.dict(os.environ, env, clear=True):
       with patch("utils.notifier.FeishuNotifier.send", return_value=True):
           result = agent.run(step1_result, step2_result, step3_result, step4_high_risk, step_dir)
   assert result.get("guardian_alerted") is False

test_step5_json_written(agent, step1_result, step2_result, step3_result, step4_medium_risk, step_dir)：
   with patch("utils.notifier.FeishuNotifier.send", return_value=True):
       agent.run(step1_result, step2_result, step3_result, step4_medium_risk, step_dir)
   step_file = step_dir / "step5.json"
   assert step_file.exists()
   data = json.load(open(step_file, encoding="utf-8"))
   assert data["step"] == 5
   assert data["agent"] == "intervention"

注意：每个需要 webhook 推送的测试，需在运行前设置 agent.feishu.webhook_url（或 agent.dingtalk.webhook_url）为非空字符串，否则 Agent5 内的 `if self.feishu.webhook_url:` 条件为 False，send 不会被调用。
  </action>
  <verify>cd D:/个人项目/mutiagent_trea && python -m pytest tests/test_agent5_intervention.py -v 2>&amp;1 | head -60</verify>
  <done>pytest 收集到 9 个测试，无 ImportError，执行完毕</done>
</task>

<task type="tdd">
  <name>Task 2: GREEN — 验证所有 Agent5 测试通过，修正 mock 路径和环境变量清理</name>
  <files>tests/test_agent5_intervention.py</files>
  <action>
运行完整测试套件，处理可能的问题：

1. mock 路径问题：
   - 若 patch("utils.notifier.FeishuNotifier.send") 不生效，改为 patch.object(agent.feishu, "send", return_value=True)
   - 确认 patch 对象是 agent 实例的 feishu 属性

2. GUARDIAN_FEISHU 环境变量测试：
   - Agent5.__init__ 在实例化时读取 env var（self.feishu = FeishuNotifier(os.getenv(...))）
   - _alert_guardian 在运行时读取 os.getenv("GUARDIAN_FEISHU", "")
   - 因此 patch.dict(os.environ, {"GUARDIAN_FEISHU": "..."}) 在 run() 调用期间生效即可

3. task_id 冲突：step4_medium_risk 和 step4_low_risk 使用不同 task_id，但 step1_result fixture 固定 task_id="test-a5-001"
   - 解决：各测试中不强依赖 task_id 一致性（step4 的 task_id 字段不影响 Agent5 逻辑，Agent5 从 step1_result 取 task_id）

4. asyncio 问题：若 _run_async 遇到已有事件循环（pytest-asyncio 等），确认 notifier.py 的 _run_async 兼容性。若出现 RuntimeError，patch 层面直接替换 send 方法可规避。

5. 报告路径验证：reports_dir 是 tmp_path 下的目录，但 _generate_report 使用 self.reports_dir（在 agent fixture 中已传入 str(reports_dir)）。验证 report_path 时需考虑时间戳子目录（YYYY-MM-DD）。

运行：python -m pytest tests/test_agent5_intervention.py -v --tb=short
确认全部 PASSED 后运行完整 Phase 4 测试：
python -m pytest tests/test_agent3_discrimination.py tests/test_agent4_assessment.py tests/test_agent5_intervention.py -v --tb=short
  </action>
  <verify>cd D:/个人项目/mutiagent_trea && python -m pytest tests/test_agent3_discrimination.py tests/test_agent4_assessment.py tests/test_agent5_intervention.py -v --tb=short 2>&amp;1</verify>
  <done>三个测试文件全部通过，汇总输出 "X passed, 0 failed"（X >= 27）</done>
</task>

</tasks>

<verification>
python -m pytest tests/test_agent3_discrimination.py tests/test_agent4_assessment.py tests/test_agent5_intervention.py -v --tb=short
预期：27+ passed, 0 failed
验证覆盖：INTERV-01（中风险 Webhook 调用）、INTERV-02（报告文件存在且含正确内容）、INTERV-03（极高风险监护人联动）、INTERV-04（awaiting_feedback=True）
</verification>

<success_criteria>
1. tests/test_agent5_intervention.py 文件存在，含 9 个测试
2. 所有 Webhook 测试使用 mock，不依赖真实飞书/钉钉账号
3. 覆盖：中风险推送（INTERV-01）、报告生成（INTERV-02）、监护人联动（INTERV-03）、反馈标志（INTERV-04）
4. Phase 4 三个文件合并运行全部通过
</success_criteria>

<output>
完成后创建 .planning/phases/04-Agent3-5验证/04-03-SUMMARY.md
</output>
