---
phase: 06-自我迭代验证
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_reflector.py
autonomous: true
requirements: [REFLECT-01, REFLECT-02, REFLECT-03]

must_haves:
  truths:
    - "ReflectorAgent._load_execution_chain 能从 .pipeline/tasks/{id}/ 读取 step1~step5.json 并构建完整链路字典"
    - "ReflectorAgent._write_reflection_report 写入 memory/reflections/YYYY-MM-DD-case-{id}.md，文件包含 root_cause 和 new_rules 段落"
    - "ReflectorAgent._update_capabilities 向 memory/capabilities.md 追加新规则条目"
    - "reflect() 主入口在 mock Claude 后能返回 status=success 并包含 reflection_file 和 rules_added 字段"
  artifacts:
    - path: "tests/test_reflector.py"
      provides: "ReflectorAgent TDD 测试套件"
      exports: ["test_load_execution_chain", "test_write_reflection_report", "test_update_capabilities", "test_reflect_end_to_end"]
  key_links:
    - from: "tests/test_reflector.py"
      to: "memory/reflector.py"
      via: "from memory.reflector import ReflectorAgent"
      pattern: "ReflectorAgent"
---

<objective>
为 memory/reflector.py 中的 ReflectorAgent 编写 TDD 测试套件，覆盖链路读取、反思报告写入、capabilities.md 更新三个核心行为。

Purpose: 验证 REFLECT-01（链路读取）、REFLECT-02（反思文件写入）、REFLECT-03（capabilities 更新）在不调用真实 Claude API 的情况下全部通过。
Output: tests/test_reflector.py，4 个测试函数，全部通过。
</objective>

<execution_context>
@C:/Users/EDY/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/EDY/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@memory/reflector.py
@agents/agent4_assessment.py
@tests/test_agent4_assessment.py
</context>

<tasks>

<task type="tdd">
  <name>Task 1: _load_execution_chain 链路读取测试</name>
  <files>tests/test_reflector.py, memory/reflector.py</files>
  <action>
    RED 阶段：在 tests/test_reflector.py 中编写 test_load_execution_chain：
    - 使用 tmp_path fixture，在 tmp_path/.pipeline/tasks/test-001/ 下创建 step1.json~step5.json，每个包含不同 JSON 内容
    - 初始化 ReflectorAgent（传入 tmp_path 作为根目录）
    - 调用 _load_execution_chain("test-001")
    - 断言返回 dict，键 "steps" 存在，"steps" 下包含 "step1"~"step5"
    - 额外断言：删除 step3.json，重新调用，对应值为空 dict {} 而非抛出异常

    GREEN 阶段：确认 memory/reflector.py 中已有 _load_execution_chain 实现，若缺失则补全最小实现使测试通过。

    mock 要求：无需 mock，纯文件系统操作。
    隔离要求：全部使用 tmp_path，不读取真实 .pipeline/ 目录。
  </action>
  <verify>cd D:/个人项目/mutiagent_trea && python -m pytest tests/test_reflector.py::test_load_execution_chain -v</verify>
  <done>test_load_execution_chain PASSED，缺失步骤文件返回空 dict 而非异常，REFLECT-01 满足</done>
</task>

<task type="tdd">
  <name>Task 2: _write_reflection_report 报告写入测试</name>
  <files>tests/test_reflector.py, memory/reflector.py</files>
  <action>
    RED 阶段：在 tests/test_reflector.py 中编写 test_write_reflection_report：
    - 构造 reflection_data = {"root_cause": "测试根因", "analysis": "详细", "missed_features": ["特征A"], "new_rules": [{"rule": "规则1", "rationale": "依据1"}], "prevention": "预防措施"}
    - 初始化 ReflectorAgent（tmp_path 为根目录）
    - 调用 _write_reflection_report("test-002", "用户反馈", {}, reflection_data)
    - 断言写入文件路径格式匹配 reflections_dir/YYYY-MM-DD-case-test-002.md（使用 glob 或正则）
    - 读取文件内容，断言包含字符串 "测试根因" 和 "规则1"

    GREEN 阶段：确认 memory/reflector.py 中已有 _write_reflection_report 实现，若缺失则补全使测试通过。

    隔离要求：全部使用 tmp_path，reflections 目录由 ReflectorAgent 在 tmp_path 下自动创建。
  </action>
  <verify>cd D:/个人项目/mutiagent_trea && python -m pytest tests/test_reflector.py::test_write_reflection_report -v</verify>
  <done>test_write_reflection_report PASSED，文件路径格式正确，内容包含 root_cause 和 new_rules 文本，REFLECT-02 满足</done>
</task>

<task type="tdd">
  <name>Task 3: _update_capabilities 规则追加测试</name>
  <files>tests/test_reflector.py, memory/reflector.py</files>
  <action>
    RED 阶段：在 tests/test_reflector.py 中编写 test_update_capabilities：
    - 在 tmp_path 创建初始 capabilities.md，内容为任意占位文本（"# Capabilities\n初始内容"）
    - 构造 reflection_data = {"new_rules": [{"rule": "规则A", "rationale": "依据A"}, {"rule": "规则B", "rationale": "依据B"}]}
    - 初始化 ReflectorAgent（tmp_path 为根目录）
    - 调用 _update_capabilities("test-003", reflection_data)
    - 断言返回整数 == 2（等于 new_rules 数量）
    - 读取文件内容，断言包含 "规则A" 和 "规则B"，且原始 "初始内容" 仍存在（追加而非覆盖）

    GREEN 阶段：确认 memory/reflector.py 中已有 _update_capabilities 实现，若缺失则补全使测试通过。

    隔离要求：capabilities.md 路径指向 tmp_path 内，不修改真实 memory/capabilities.md。
  </action>
  <verify>cd D:/个人项目/mutiagent_trea && python -m pytest tests/test_reflector.py::test_update_capabilities -v</verify>
  <done>test_update_capabilities PASSED，返回值等于规则数，原有内容不被覆盖，REFLECT-03 满足</done>
</task>

<task type="tdd">
  <name>Task 4: reflect() 主入口端到端测试（mock Claude）</name>
  <files>tests/test_reflector.py, memory/reflector.py</files>
  <action>
    RED 阶段：在 tests/test_reflector.py 中编写 test_reflect_end_to_end：
    - 使用 tmp_path 搭建完整目录结构（.pipeline/tasks/test-004/ 含 step1~step5.json，reflections 目录，capabilities.md）
    - 使用 unittest.mock.patch("memory.reflector.anthropic.Anthropic") mock Claude 客户端
    - mock 返回的 messages.create().content[0].text 为合法 JSON 字符串：
      {"root_cause": "测试根因", "analysis": "...", "missed_features": [], "new_rules": [{"rule": "mock规则", "rationale": "mock依据"}], "prevention": "..."}
    - 初始化 ReflectorAgent（tmp_path 为根目录）
    - 调用 reflect("test-004", "测试反馈")
    - 断言 result["status"] == "success"
    - 断言 result["reflection_file"] 是存在的文件路径（os.path.exists 为 True）
    - 断言 result["rules_added"] >= 0

    GREEN 阶段：确认 memory/reflector.py 中 reflect() 已有实现，若缺失则补全使测试通过。

    隔离要求：patch 路径必须是 "memory.reflector.anthropic.Anthropic"，不调用真实 Claude API。
  </action>
  <verify>cd D:/个人项目/mutiagent_trea && python -m pytest tests/test_reflector.py -v</verify>
  <done>4 个测试全部 PASSED，0 ERROR，0 FAILED，无真实 API 调用</done>
</task>

</tasks>

<verification>
运行命令：
```
cd D:/个人项目/mutiagent_trea && python -m pytest tests/test_reflector.py -v
```
预期：4 个测试全部 PASSED，0 ERROR，0 FAILED。
</verification>

<success_criteria>
- tests/test_reflector.py 存在，包含 4 个测试函数
- pytest tests/test_reflector.py 全部通过
- REFLECT-01、REFLECT-02、REFLECT-03 需求均有对应测试断言
- 无真实外部 API 调用（Claude mock 验证）
</success_criteria>

<output>
完成后创建 .planning/phases/06-自我迭代验证/06-01-SUMMARY.md
</output>
